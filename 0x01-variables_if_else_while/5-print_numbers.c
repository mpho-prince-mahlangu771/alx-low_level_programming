#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints all single digits # starting at 0
 *
 * Return: Always 0 (sucess)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}			
